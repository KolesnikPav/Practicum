создал массив и указал размер массива равным 10

создал объект типа Random

затем запустил цикл for

инициализатором является n = 0

условие цикла n < 10, если оно выполняется то программа вызывает рандом и генерирует 
любое целое число (если условие не выполняется программа прекращает работу)

когда программа сгенерировала целое число, запускается следующее условие,
это число делится на 2 и если остаток 0, то число выводится на экран, затем n++ (переходим на следующий индекс массива)
и for перезапускается до тех пока выполняется условие n < 10

если остаток не 0, то на экран ничего не выводится, n++, for перезапускается